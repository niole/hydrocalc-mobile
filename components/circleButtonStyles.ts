import { StyleSheet } from 'react-native';

export const svgSizes = {
  big: 60,
  small: 30
};

export const styles = StyleSheet.create({
  primary: {
    color: 'teal',
  },
  destructive: {
    color: 'red',
  },
  container: {
    borderRadius: 100,
    alignContent: 'center',
    width: 'fit-content',
  }
});
